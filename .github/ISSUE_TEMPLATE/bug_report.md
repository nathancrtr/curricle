---
name: Bug report
about: Something the compiler, the served app, the factory or the wizard did that it should not have
labels: bug
---

**What happened**

**What you expected**

**How to reproduce**
The smallest course, command or click sequence that shows it. If a compile
is involved, paste the `Issue` lines the compiler printed — every one names a
place.

**Where**
- curricle commit or tag:
- Python version:
- Postgres version (served app / tests only):
- Mode: static pages / `serve` / `work` / `mcp` / `factory`

**Anything else**
If it involves money (the factory or the wizard's paid stages), say what the
gate showed and what the ledger recorded.

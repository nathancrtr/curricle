"""tinylang — the language you are building.

The course seeds this package with the one piece worth fixing in advance: the
token shape in `lexer.py`. Everything else — the parser, the evaluator, the
REPL — you create as the curriculum reaches it, so the package grows from
almost nothing into a language over eight weeks.
"""
